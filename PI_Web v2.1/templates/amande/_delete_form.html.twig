<form method="post" action="{{ path('app_amande_delete', {'id': amande.id}) }}" onsubmit="return confirm('Voulez vous supprimer cette amande?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amande.id) }}">
    <button class="btn">Delete</button>
</form>
