{% extends'baseBack.html.twig' %}
{% block body %}
    <div class="content-wrapper">
        <div class="container-fluid">

            <div class="row mt-3">
                <div class="col-lg-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Liste des clients</h5>
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                    <tr>

                                    <tr>
                                        <th>ID </th>
                                        <th>Nom</th>
                                        <th>Prénom</th>
                                        <th>Numéro de télephone</th>
                                        <th>Immatricule voiture</th>
                                        <th>Insta monnaie</th>
                                        <th>Login</th>
                                        <th>Password</th>
                                        <th>Statut</th>
                                        <th>Actions</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for client in clients %}
                                        <tr>
                                            <td>{{ client.id }}</td>
                                            <td>{{ client.nom }}</td>
                                            <td>{{ client.prenom }}</td>
                                            <td>{{ client.numtel }}</td>
                                            <td>{{ client.voitmat }}</td>
                                            <td>{{ client.instamonnaie }}</td>
                                            <td>{{ client.login }}</td>
                                            <td>{{ client.password }}</td>
                                            <td>{{ client.statut }}</td>
                                            <td>
                                                <a href="{{ path('app_client_show', {'id': client.id}) }}">show</a>
                                                <a href="{{ path('app_client_edit', {'id': client.id}) }}">edit</a>
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="10">no records found</td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>


                            </div>
                        </div>
                    </div>
                    <a href="{{ path('app_client_new') }} " style="float: right;">Créer Nouveau client</a>
                </div>
            </div>
        </div>
    </div>


{% endblock %}


