<form method="post" action="{{ path('app_admin_delete', {'id': admin.id}) }}" onsubmit="return confirm('Voulez vous supprimer admin');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin.id) }}">
    <button class="btn">Supprimer</button>
</form>
