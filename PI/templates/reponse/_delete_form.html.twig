<form method="post" action="{{ path('reponse_delete', {'id_rep': reponse.id}) }}" onsubmit="return confirm('Voulez vous supprimez la réponse?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.id) }}">
    <button class="btn">Supprimer</button>
</form>
