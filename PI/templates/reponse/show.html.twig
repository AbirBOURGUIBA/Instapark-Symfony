{% extends'baseBack.html.twig' %}
{% block body %}
    <div class="content-wrapper">
        <div class="container-fluid">

            <div class="row mt-3">
                <div class="col-lg-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Liste des réponses</h5>
                            <div class="table-responsive">
                                <table class="table table-striped">
    <table class="table">
        <tbody>
            <tr>
                <th>Id réponse</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Réponse</th>
                <td>{{ reponse.rps }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse') }}">Lise des réponse</a>

    <a href="{{ path('reponse_edit', {'id': reponse.id}) }}">Modifier</a>

    {{ include('reponse/_delete_form.html.twig') }}
    </div>
    </div>
    </div>

    </div>

{% endblock %}

